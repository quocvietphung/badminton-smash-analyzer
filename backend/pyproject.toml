[project]
name = "smashlab-backend"
version = "0.1.0"
description = "FastAPI pose-metric analysis backend for SmashLab"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
